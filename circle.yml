

# Add some environment variables
machine:
    environment:
            COVERALLS_REPO_TOKEN: uhH9cjNDKk7BZkvkAJFl8NSvcoqVDCyAf
    java:
        version: oraclejdk8
dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "tools"
    - echo y | android update sdk --no-ui --all --filter "build-tools-23.0.2"

test:
  override:
    - ./gradlew test -PdisablePreDex
    # copy test results
    - ./gradlew testDebugUnitTestCoverage coveralls
    - cp app/build/reports/tests/debug $CIRCLE_TEST_REPORTS/AppUnitTests
    - cp app/build/reports/jacoco $CIRCLE_TEST_REPORTS/AppUnitTestsReports
    - ./gradlew testDebugUnitTestCoverage coveralls